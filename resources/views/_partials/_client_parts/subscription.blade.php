<div class="subscription">
    <div class="subscription__in">
        <div class="subscription__title">{{ $title }}</div>
        <div class="subscription__subtitle">{{ $subtitle }}</div>
        <form action="#" class="subscription__form">
            <div class="subscription__input general__input">
                <input type="email" placeholder="Ваш e-mail">
            </div>
            <button class="subscription__button button button--primary">Подписаться</button>
        </form>
    </div>
</div>